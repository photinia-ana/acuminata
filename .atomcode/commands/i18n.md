# /i18n — Internationalization Key Sync

Check and sync translation keys between `locales/en.json` and `locales/zh-CN.json`.

## Steps

1. **Read both locale files**:
   - `C:\Roy\workspace\browser-history-tracker\locales\en.json`
   - `C:\Roy\workspace\browser-history-tracker\locales\zh-CN.json`

2. **Compare keys** (deep comparison, not just top-level):
   - Find keys in `en.json` missing from `zh-CN.json`
   - Find keys in `zh-CN.json` missing from `en.json`

3. **Report missing keys** as a table:

| Key | Present In | Missing From |
|-----|-----------|-------------|

4. **Auto-fix** (if user confirms):
   - For keys missing in `zh-CN.json`: copy the English value as placeholder, prefix with `// TODO: translate`
   - For keys missing in `en.json`: flag them for manual review (don't auto-delete)

5. **Verify**: Re-read both files and confirm they now have identical key sets.
